# La app Next.js vive en la subcarpeta web/ y se exporta como sitio estático.
# El backend (menú, pedidos, clientes, analítica) vive en Supabase.
[build]
  base = "web"
  command = "npm run build"
  publish = "out"
